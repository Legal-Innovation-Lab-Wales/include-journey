<p class="mb-0">
  <% if current_user %>
    <%= "You" %> 
  <% else %>
    <%= @user.first_name%>
  <% end %>
  have a total of <%= "#{pluralize(@count, 'upload')}" %>, with
  <%= @uploads_in_last_30_days %> made in the last 30 days. </p>
  <p class="mb-0">
  <% if current_user %>
    <%= "Your" %> 
  <% else %>
    <%= @user.first_name%>
  <% end %>
  uploads consist of  <%= "#{pluralize(@image_uploads, 'image')}" %> and <%= "#{pluralize(@pdf_uploads, 'PDF file')}" %>.</p>
  <p class="mb-0">
  <% if current_user %>
    <%= "You" %> 
  <% else %>
    <%= @user.first_name%>
  <% end %>
  uploaded <%= "#{pluralize(@user_uploads, 'file')}" %> while team members uploaded <%= "#{pluralize(@team_member_uploads, 'file')}" %> 
</p>
<p>
  Click the 'Add Upload' button if you want to add a new upload or you can view 
   <% if current_user %>
    <%= "your" %> 
  <% else %>
    <%= "#{@user.first_name}'s"%>
  <% end %>
   previous uploads.
</p>