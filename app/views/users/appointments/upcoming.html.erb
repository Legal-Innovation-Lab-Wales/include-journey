<main class="container user">
  <%= render 'shared/back_btn', path: authenticated_user_root_path, text: 'Dashboard',
             next_path: new_appointment_path, next_text: 'New Appointment' %>
  <section class="row appointments">
    <h3 class="mb-0">Upcoming appointments</h3>
    <div class="container">
      <div class="row">
        <% @appointments.each do |appointments| %>
          <div class="col-12 col-md-4">
            <div class="card mt-3 text-al">
              <div class="card-header">
                <span>
                  <%= appointments.what %> - In <%= distance_of_time_in_words(Time.now, appointments.start) %>
                </span>
                <span>
                  <% if appointments.team_member.present? %>
                    <p>Created by <%= appointments.team_member.full_name %></p>
                  <% else %>
                    <span>
                      <%= link_to edit_appointment_path(appointments.id) do %>
                        <i class="fas fa-edit" data-toggle="tooltip" data-placement="bottom" title="Edit"></i>
                      <% end %>
                    </span>
                    <span>
                      <%= link_to appointment_path(appointments.id), method: :delete, data: { confirm: 'Are you sure?' } do %>
                        <i class="fas fa-trash-alt" data-toggle="tooltip" data-placement="bottom" title="Delete"></i>
                      <% end %>
                    </span>
                  <% end %>
                </span>
              </div>
              <div class="card-body">
                <div class="container p-0">
                  <div class="row">
                    <div class="col"><i class="fas fa-user"></i></div>
                    <div class="col"><%= appointments.who_with %></div>
                  </div>
                  <div class="row">
                    <div class="col"><i class="fas fa-location-arrow"></i></div>
                    <div class="col"><%= appointments.where %></div>
                  </div>
                  <div class="row">
                    <div class="col"><i class="fas fa-clock"></i></div>
                    <div class="col"><%= appointments.time_on_date %></div>
                  </div>
                  <div class="row">
                    <div class="col"><i class="fas fa-hourglass-start"></i></div>
                    <div class="col"><%= appointments.duration %></div>
                  </div>
                </div>
              </div>
            </div>
          </div>
        <% end %>
      </div>
    </div>
  </section>
  <section class="row">
    <div class="col-2 col-md-4"></div>
    <div class="col-8 col-md-4">
      <%= link_to 'View Past Appointments', appointments_path, class: 'btn btn-primary' %>
    </div>
    <div class="col-2 col-md-4"></div>
  </section>
</main>
